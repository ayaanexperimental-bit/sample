# Admin and Shop Template Selector Report

Implemented:
- Shop Builder imports `getSkinsForShopSelector()`.
- Admin Website Creator imports `getSkinsForAdminSelector()`.
- Raw `coachTemplateThemes.map()` is no longer used by selectors.
- Option labels use `publicName`.
- Helper text shows design story, background type, motion level, and mobile-safe status.

Behavior preserved:
- Selecting a skin patches only `selectedThemeId`.
- Content is not reset.
- AI copy is not regenerated by skin selection.
- No redirect occurs.
- Publish continues to use saved `selectedThemeId`.
- Payment flow is untouched.

Exposure rule:
- Only production_ready skins are visible to normal Admin/Shop selectors.
